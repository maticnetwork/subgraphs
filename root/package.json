{
  "name": "root-matic-subgraph",
  "license": "MIT",
  "scripts": {
    "graph": "graph",
    "codegen": "graph codegen",
    "build": "graph build",
    "prepare:mainnet": "mustache config/mainnet.json subgraph.template.yaml > subgraph.yaml",
    "prepare:goerli": "mustache config/goerli.json subgraph.template.yaml > subgraph.yaml",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ nglglhtr/token-subgraph",
    "create-local": "graph create --node http://localhost:8020/ maticnetwork/goerli-matic-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ maticnetwork/goerli-matic-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 maticnetwork/goerli-matic-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "0.18.0"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.18.0",
    "mustache": "4.0.1"
  }
}
